// Community category icons resolve through the shared, tree-shakeable icon
// registry. getIconComponent is kept as the module's local name for its callers.
export { getIcon as getIconComponent, Bench } from '@/lib/icons'
export type { IconProps } from '@/lib/icons'
